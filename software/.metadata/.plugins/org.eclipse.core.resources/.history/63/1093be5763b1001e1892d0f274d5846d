#include "system.h"
#include "altera_avalon_pio_regs.h"
#include <time.h>

int main(void){

		int tab[4];
		tab[0]= 001;
		tab[2]= 200;

for (int i=0; i<size(2); i++)
	{
		IOWR_ALTERA_AVALON_PIO_DATA(LED_OUT_BASE,tab[i]);
	}



	return 0;
};
